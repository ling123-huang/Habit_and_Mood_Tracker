<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HabitMood" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HabitMood" parent="Base.Theme.HabitMood" />

    <!-- habit 추가 팝업  -->
    <style name="Theme.HabitMood.Dialog" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <!-- 달력 전체 테마 (선택된 날짜 색상 변경) -->
    <style name="CalenderViewCustom" parent="Theme.MaterialComponents.DayNight">
        <!-- 선택된 날짜의 동그라미 색상 -->
        <item name="colorAccent">@color/light_green</item>
        <item name="colorPrimary">@color/light_green</item>
    </style>

    <!-- 날짜 숫자 스타일 (1, 2, 3...) -->
    <style name="CalenderViewDateCustom" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/comfortaa</item>
    </style>

    <!-- 요일 글자 스타일 (Sun, Mon...) -->
    <style name="CalenderViewWeekCustom" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#006064</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>